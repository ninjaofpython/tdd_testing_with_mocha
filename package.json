{
  "name": "tdd_mocha",
  "version": "1.0.0",
  "description": "testing suite",
  "main": "index.js",
  "scripts": {
    "test": "mocha && mocha test --require blanket --reporter doc > coverage.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "blanket": "^1.2.3",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }, 
    "config":{
        "blanket":{
            "pattern": [ "index.js" ],
            "data-cover-never": "node-modules"
        }
    }
}
